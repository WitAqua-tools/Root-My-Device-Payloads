{
  "$comment": [
    "The KernelSU late-load build for this target. A run of it reached a loaded,",
    "answering KernelSU on the device: module Live, /data/adb/ksud installed,",
    "`su` returning uid 0 in u:r:ksu:s0, and SELinux back to Enforcing.",
    "",
    "ddkImage below is NOT what produced that module, and this file cannot yet",
    "say what did. The generic android12-5.10 DDK is only an approximation of",
    "this kernel -- Meta builds its own rather than shipping GKI -- and a module",
    "built from it rebooted the device on load every time, three runs out of",
    "three, while resolving every symbol. CFI, LTO, SCS and PAC all match; what",
    "does not is 104 options that are =y on the device and absent from the DDK,",
    "four of them guarding task_struct fields (KASAN, PREEMPT_NOTIFIERS,",
    "TASK_DELAY_ACCT, and UBSAN_TRAP compiling traps into the module).",
    "",
    "What produced the module that works: Meta's own source for this build,",
    "",
    "    kernel/oculus-linux-kernel, branch oculus-quest3-kernel-master,",
    "    HEAD 'Oculus Quest 3 kernel project build: 5234532.3540.520'",
    "    = ro.build.version.incremental 52345320035400520",
    "",
    "with the device's own /proc/config.gz as .config, built with the DDK",
    "image's toolchain (its Android clang r416183b builds 5.10; the host's",
    "clang-21 does not) and used as KDIR for an external module build. A full",
    "kernel build is not needed and does not finish here -- Qualcomm's vendor",
    "drivers fail on tracepoint includes -- but modules_prepare alone is not",
    "enough either: KernelSU's selinux part needs the generated",
    "security/selinux/flask.h, which the failed full build produces before it",
    "stops.",
    "",
    "That build now happens in Root-My-Device-Kernel, and this entry references",
    "its result rather than building one -- see prebuiltModule below. ddkImage",
    "still names the closest KMI, which is what ksud is built with and what",
    "says which KMI this is; the module does not come from it.",
    "",
    "Measured on the device and worth keeping:",
    "  - CONFIG_MODULES=y and CONFIG_MODULE_SIG unset, so unsigned modules load.",
    "  - CONFIG_MODVERSIONS=y, answered as everywhere else here by an empty",
    "    Module.symvers and a zero-length __versions section, with ksud",
    "    resolving the 48 unexported symbols from /proc/kallsyms.",
    "  - kptr_restrict is 2, which hides every kallsyms address from root as",
    "    well; the bootstrap helper now sets 0 across the load and restores it.",
    "  - The audit decides the KMI, not the ship date:",
    "    extract_target_symvers.py against a vmlinux recovered from the running",
    "    kernel, then audit_module_against_target.py. MISSING_SYMBOL is fatal,",
    "    MISSING_EXPORT is not. android14-6.1 gives 10, android12-5.10 gives 1,",
    "    the real source gives 0.",
    "",
    "devices/quest3 is upstream 953b403a backported onto the pinned 32525. It",
    "keeps the tracepoint mark on /system/bin/stub_zygote, which is how Horizon",
    "OS starts the zygote; without it a `ksud soft-reboot` brings back a zygote",
    "no KernelSU hook fires for, and the manager dies with SIGSYS on its first",
    "supercall. The set exists at 32514 and 32525 and deliberately not at",
    "32567, which already carries the commit: a pin move to it fails this build",
    "until the name is taken out of patchSets, which is the reminder wanted."
  ],
  "id": "quest3",
  "kmi": "android12-5.10",
  "ddkImage": "ghcr.io/ylarod/ddk-min:android12-5.10-20260313",
  "kernelRelease": "5.10.240-g55be3759aea4",
  "patchSets": ["devices/quest3"],
  "config": [],
  "managerPackage": "me.weishu.kernelsu",
  "prebuiltModule": {
    "$comment": [
      "Built from Meta's own kernel source in Root-My-Device-Kernel, not from",
      "the ddkImage above. The generic android12-5.10 DDK resolves every",
      "symbol and still reboots this device on load, three runs out of three;",
      "the image is named only so the entry says which KMI this is and so the",
      "toolchain that builds ksud is the one everything else uses.",
      "",
      "The digest is what makes this a reference to a build rather than trust",
      "in a URL, and the action checks vermagic, __versions and the manager",
      "certificate on top of it -- the same things it proves about a module it",
      "built itself."
    ],
    "url": "https://github.com/WitAqua-tools/Root-My-Device-Kernel/releases/download/modules-20260802-ed67fc6/kernelsu-quest3.ko",
    "sha256": "591f865f892a0b9bcf3a43fe7e1d9d11efd4036df11bd3e100be84f61f317b29"
  },
  "manager": {
    "$comment": [
      "The official manager cannot be used here. It rewrites /data/adb/ksud",
      "with the copy bundled in its own APK the first time it runs, which puts",
      "an unpatched daemon back: measured on this device, opening it undid",
      "common/0004 and the next soft restart went back to leaving stale lspd",
      "and sui behind. Nothing in the log says the daemon was replaced.",
      "",
      "A soft restart is the only restart this target has -- a real reboot",
      "loses root -- so that is not a corner case, it is the normal path.",
      "",
      "This build is upstream 32525 with the common patch set applied to ksud,",
      "from WitAqua-tools/KernelSU, and it keeps the package name so nothing",
      "else has to change. It cannot be installed over the official manager;",
      "that one has to be uninstalled first."
    ],
    "url": "https://github.com/WitAqua-tools/KernelSU/releases/download/manager-32525-b0bc817b/KernelSU_witaqua_32525.apk",
    "versionName": "32525 (WitAqua)",
    "versionCode": 32525,
    "required": true,
    "reason": "The official manager replaces ksud with its own copy and undoes the fix that keeps modules working across a soft restart, which is the only restart this device has."
  },
  "managerSignature": {
    "$comment": [
      "The certificate the module accepts as a manager, in its second slot --",
      "KSU_EXPECTED_SIZE2 / KSU_EXPECTED_HASH2. Upstream's stays in the first,",
      "so a module built from this still recognises the official manager too.",
      "Without this the manager above is simply not seen, with nothing logged.",
      "The pair is the DER size and sha256 of Android-Keys/kernelsu-manager,",
      "and it is not a secret."
    ],
    "size": "0x0524",
    "hash": "a66068cd6cc875e8d43334d1bf70eadd0ab8ad6096d8d004e39e7b6a87cdf6f0"
  }
}
